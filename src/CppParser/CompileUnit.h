#pragma once
#include <string>
#include <vector>

#include "PreCompiledLexer.h"

class CompileUnit {
    std::vector<std::string> includes_path;
    std::vector<std::string> source_files;

   public:
};